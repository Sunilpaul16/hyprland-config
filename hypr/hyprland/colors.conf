# exec = export SLURP_ARGS='-d -c FCD7FBBB -b 5B405D44 -s 00000000'

general {
    col.active_border = rgba(E6E0EC39)
    col.inactive_border = rgba(948E9F30)
}

misc {
    background_color = rgba(14121AFF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(14121AFF)
        col.text = rgba(E6E0ECFF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(E6E0EC), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(E6E0EC), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(E6E0EC), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(88D7AAAA) rgba(88D7AA77),pinned:1