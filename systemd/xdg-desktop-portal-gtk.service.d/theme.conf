[Service]
# Written by switchwall/setscheme so the portal can use the appropriate GTK 3
# base without changing the theme of unrelated GTK applications.
EnvironmentFile=-%h/.local/state/quickshell/gtk-portal.env
