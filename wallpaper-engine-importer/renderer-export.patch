diff --git a/src/WallpaperEngine/Application/WallpaperApplication.cpp b/src/WallpaperEngine/Application/WallpaperApplication.cpp
index c9107af..1fc29f0 100644
--- a/src/WallpaperEngine/Application/WallpaperApplication.cpp
+++ b/src/WallpaperEngine/Application/WallpaperApplication.cpp
@@ -843,12 +843,6 @@ void WallpaperApplication::setup () {
     if (this->m_renderContext->getWallpapers ().size () > 1) {
 	sLog.exception ("Demo mode only supports one background");
     }
-
-    int width = this->m_renderContext->getWallpapers ().begin ()->second->getWidth ();
-    int height = this->m_renderContext->getWallpapers ().begin ()->second->getHeight ();
-    std::vector<uint8_t> pixels (width * height * 3);
-    bool initialized = false;
-    int frame = 0;
 #endif /* DEMOMODE */
 }
 
@@ -900,6 +894,11 @@ void WallpaperApplication::render () {
 	}
 
 #if DEMOMODE
+	static int width = 0;
+	static int height = 0;
+	static std::vector<uint8_t> pixels;
+	static bool initialized = false;
+	static int frame = 0;
 	// wait for a full render cycle before actually starting
 	// this gives some extra time for video and web decoders to set themselves up
 	// because of size changes
@@ -907,7 +906,7 @@ void WallpaperApplication::render () {
 	    if (!initialized) {
 		width = this->m_renderContext->getWallpapers ().begin ()->second->getWidth ();
 		height = this->m_renderContext->getWallpapers ().begin ()->second->getHeight ();
-		pixels.reserve (width * height * 3);
+		pixels.resize (width * height * 3);
 		init_encoder ("output.webm", width, height);
 		initialized = true;
 	    }
@@ -997,4 +996,4 @@ void WallpaperApplication::setDestinationFramebuffer (GLuint framebuffer) {
     };
 }
 
-GLuint WallpaperApplication::getDestinationFramebuffer () const { return this->m_destinationFramebuffer; }
\ No newline at end of file
+GLuint WallpaperApplication::getDestinationFramebuffer () const { return this->m_destinationFramebuffer; }
diff --git a/src/recording.cpp b/src/recording.cpp
index c49ef69..d6955c5 100644
--- a/src/recording.cpp
+++ b/src/recording.cpp
@@ -20,7 +20,15 @@ extern "C" {
 }
 
 const int FPS = 30;
-const int FRAME_COUNT = FPS * 5;
+const int FRAME_COUNT = FPS * [] () {
+    const char* value = std::getenv ("WE_EXPORT_SECONDS");
+    if (value == nullptr) {
+        return 5;
+    }
+    char* end = nullptr;
+    const long seconds = std::strtol (value, &end, 10);
+    return end != value && *end == '\0' && seconds >= 1 && seconds <= 600 ? static_cast<int> (seconds) : 5;
+} ();
 int WIDTH = 0;
 int HEIGHT = 0;
 int SOURCE_WIDTH = 0;
@@ -38,11 +46,10 @@ AVFrame* video_frame = nullptr;
 AVFrame* rgb_frame = nullptr;
 
 int init_encoder (const char* output_file, int sourceWidth, int sourceHeight) {
-    float factor = 512.0f / (float)sourceWidth;
     SOURCE_WIDTH = sourceWidth;
     SOURCE_HEIGHT = sourceHeight;
-    WIDTH = (float)sourceWidth * factor;
-    HEIGHT = (float)sourceHeight * factor;
+    WIDTH = sourceWidth;
+    HEIGHT = sourceHeight;
 
     avformat_network_init ();
 
@@ -135,10 +142,17 @@ int write_video_frame (const uint8_t* rgb_data) {
 	rgb_frame->data, rgb_frame->linesize, rgb_data, AV_PIX_FMT_RGB24, SOURCE_WIDTH, SOURCE_HEIGHT, 1
     );
 
+    if (av_frame_make_writable (video_frame) < 0) {
+	std::cerr << "Error making video frame writable" << std::endl;
+	return -1;
+    }
+
     sws_scale (
 	sws_context, rgb_frame->data, rgb_frame->linesize, 0, SOURCE_HEIGHT, video_frame->data, video_frame->linesize
     );
 
+    video_frame->pts = frame_count++;
+
     // Send the frame to the encoder
     int ret = avcodec_send_frame (video_codec_context, video_frame);
     if (ret < 0) {
@@ -146,42 +160,37 @@ int write_video_frame (const uint8_t* rgb_data) {
 	return -1;
     }
 
-    AVPacket packet;
-    av_init_packet (&packet);
-    packet.data = nullptr;
-    packet.size = 0;
-
-    // Receive the encoded packet from the encoder
-    ret = avcodec_receive_packet (video_codec_context, &packet);
-    if (ret < 0) {
-	std::cerr << "Error receiving video packet: " << ret << std::endl;
-	return -1;
+    AVPacket* packet = av_packet_alloc ();
+    while ((ret = avcodec_receive_packet (video_codec_context, packet)) >= 0) {
+	packet->stream_index = video_stream->index;
+	av_packet_rescale_ts (packet, video_codec_context->time_base, video_stream->time_base);
+	if (av_interleaved_write_frame (format_context, packet) < 0) {
+	    av_packet_free (&packet);
+	    std::cerr << "Error writing video packet" << std::endl;
+	    return -1;
+	}
+	av_packet_unref (packet);
     }
-
-    packet.stream_index = video_stream->index;
-    // Set the PTS and DTS values
-    packet.pts = av_rescale_q (frame_count, video_codec_context->time_base, video_stream->time_base);
-    packet.dts = packet.pts; // For simplicity, you can set DTS equal to PTS for now
-    packet.duration = av_rescale_q (1, video_codec_context->time_base, video_stream->time_base);
-
-    // Increment frame counter
-    frame_count++;
-    // Write the encoded video packet to the file
-    ret = av_interleaved_write_frame (format_context, &packet);
-    if (ret < 0) {
-	std::cerr << "Error writing video packet: " << ret << std::endl;
+    av_packet_free (&packet);
+    if (ret != AVERROR (EAGAIN) && ret != AVERROR_EOF) {
+	std::cerr << "Error receiving video packet: " << ret << std::endl;
 	return -1;
     }
 
-    // Ensure that the packet is freed
-    av_packet_unref (&packet);
-
     return 0;
 }
 
 int close_encoder () {
-    // Write any remaining frames (flush encoder)
-    avcodec_flush_buffers (video_codec_context);
+    // Flush delayed frames from the encoder.
+    avcodec_send_frame (video_codec_context, nullptr);
+    AVPacket* packet = av_packet_alloc ();
+    while (avcodec_receive_packet (video_codec_context, packet) >= 0) {
+	packet->stream_index = video_stream->index;
+	av_packet_rescale_ts (packet, video_codec_context->time_base, video_stream->time_base);
+	av_interleaved_write_frame (format_context, packet);
+	av_packet_unref (packet);
+    }
+    av_packet_free (&packet);
 
     // Write the trailer
     av_write_trailer (format_context);
@@ -195,4 +204,4 @@ int close_encoder () {
     return 0;
 }
 
-#endif /* DEMOMODE */
\ No newline at end of file
+#endif /* DEMOMODE */
